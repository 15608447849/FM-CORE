dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':lib_ping')
    shadowJar {
        baseName = 'MLTCP'
        classifier = null
        version = null
        manifest {
            attributes 'Main-Class': 'bottle.tcps.FTCMain'
        }
    }
}
