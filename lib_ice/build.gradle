

dependencies {
//    compile fileTree(dir: 'libs', include: ['*.jar'])
    //引用公共库 
    compile project(':lib_ping')
}

shadowJar {
    baseName = 'lib_ice'
    classifier = null
    version = 1.0
}