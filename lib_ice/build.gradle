

dependencies {
//    compile fileTree(dir: 'libs', include: ['*.jar'])
    //引用公共库 
    compile project(':lib_ping')
}

shadowJar {
    baseName = 'lib-ice'
    classifier = null
    version = null
}